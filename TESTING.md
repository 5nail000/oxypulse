# Проверка на железе

Обзор проекта — [`README.md`](README.md). API — [`docs/API.md`](docs/API.md).

**Приоритет проверки:** прошивка + веб-UI. Android — опционально, те же JSON-поля.

## Предусловия

- ESP32 с прошивкой из `firmware/`
- Браузер на телефоне/ПК, подключённый к AP `ESP32-Tester` или LAN (STA)
- Реле/клапан на GPIO 26 (опционально)
- I2C: SDA→GPIO22, SCL→GPIO23, подтяжки 4.7 kΩ к 3.3 V

Датчики можно подключать по одному: в JSON `sensors.*.ok = false`, остальное работает.

## Прошивка

```bash
cd firmware
pio run -t uploadfs
pio run -t upload
pio device monitor
```

## Веб-UI

1. Подключиться к WiFi `ESP32-Tester` / пароль `12345678`
2. Открыть `http://192.168.4.1/`
3. **Ожидание:** секция «Подключение», поллинг статуса, плитки датчиков

---

## 0. I2C — делать первым

1. Перезагрузить ESP32, смотреть Serial
2. **Ожидание:** перечислены найденные адреса I2C
3. Отсутствующий адрес — проверить питание и подтяжки, не код

---

## 1. O₂ (AO-02 + ADS1115)

1. **Ожидание:** плитка O₂ с % и mV
2. Калибровка — [`firmware/README.md`](firmware/README.md): short → `O2_OFFSET_MV`, воздух → `O2_AIR_MV`

---

## 2. Поток (SFM3300)

1. Плитка «Поток»: slm, фазы inhale/exhale
2. Продышать контур — знак потока меняется, VE растёт
3. «Сброс объёма» → `flow_reset` в логе

---

## 3. CO₂ (SCD41)

1. Первые ~10 с — «Прогрев»
2. В проветренном помещении 400–800 ppm
3. Выдох к датчику — рост на тысячи ppm

---

## 4. Давление (DPS310)

1. Плитка hPa и °C
2. Если `ok: false` — проверить SDO (0x76 vs 0x77)

---

## 5. Wellue Ring O2 S

1. Кольцо в режиме **S8-AW** (не запись `T8520_xxxx`)
2. **Ожидание:** в `/api/status` → `sensors.wellue.connected`, затем SpO₂/HR
3. В веб-UI — плитка Wellue; кнопка **Тренд** для графика

---

## 6. COOSPO H6M

1. Включить ремень, приложить датчик
2. **Ожидание:** `sensors.coospo.connected`, bpm, contact
3. R-R — отдельный канал тренда `rr`, если ремень отдаёт

---

## 7. WiFi STA

1. Кнопка **WiFi** → скан → SSID + пароль → сохранить
2. После reboot — доступ по IP в LAN (`wifi.sta.ip` в status)
3. AP остаётся для повторной настройки

---

## 8. GPIO / клапан

1. GPIO Tester → pin 26 → ON / tap 3 с
2. **Ожидание:** реле срабатывает, в status `gpio` обновляется

---

## 9. Тренды

1. На плитке датчика — **Тренд**
2. Секция «Тренды»: графики 2/5/10/60 мин
3. Reboot обнуляет точки; маска каналов сохраняется в NVS

---

## 10. Android (опционально)

1. `cd android && gradlew.bat assembleDebug`
2. WiFi → IP ESP32 или BLE → `ESP32-Tester`
3. GPIO/Servo, Wellue/COOSPO в карточке статуса
4. I2C-метрики — пока только в веб-UI
