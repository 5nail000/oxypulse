# Проверка на железе

Обзор проекта — [`README.md`](README.md). API — [`docs/API.md`](docs/API.md).

**Приоритет проверки:** прошивка + веб-UI. Android — опционально (WiFi HTTP), те же JSON-поля.

## Предусловия

- Две платы: DevKit CH340 (`firmwares/firmware_main_devkit`) и BLE-узел (`firmwares/firmware_ble_s3` или `firmware_ble_devkit`)
- UART: **GPIO16↔16**, **GPIO17↔17**, общая GND (прямая коммутация)
- Браузер на телефоне/ПК, подключённый к AP `ESP32-Tester` или LAN (STA)
- Реле/клапан на GPIO 26 (опционально)
- I2C гипоксия: SDA→GPIO22, SCL→GPIO23 (DevKit)
- I2C рабочий: SDA→GPIO18, SCL→GPIO19 (DevKit)
- Подтяжки 4.7 kΩ к 3.3 V **на каждой шине**

Датчики можно подключать по одному: в JSON `sensors.*.ok = false`, остальное работает.

## Прошивка

```bash
# BLE-узел (один из вариантов)
cd firmwares/firmware_ble_s3
pio run -t upload
# или: cd firmwares/firmware_ble_devkit

cd ../firmware_main_devkit
pio run -t uploadfs
pio run -t upload
pio device monitor
```

Сначала Serial главного узла: `UART bridge: hello from BLE node`. В `/api/status` — `sensors.bridge.ok: true`. Если `error: "uart"` — проверить TX↔RX и GND.

## Веб-UI

1. Подключиться к WiFi `ESP32-Tester` / пароль `12345678` **или** открыть UI по STA-IP в LAN (см. `wifi.sta.ip` в status)
2. Открыть `http://192.168.4.1/` (AP) или `http://<STA_IP>/` (предпочтительно, если ESP в домашней WiFi)
3. **Ожидание:** секция «Подключение», поллинг статуса, плитки датчиков

### Проверка скорости HTTP (STA)

Подставьте IP из `wifi.sta.ip` (например `192.168.31.73`):

```powershell
curl -w "`nTIME: %{time_total}s`n" http://192.168.31.73/app.js -o NUL
curl -w "`nTIME: %{time_total}s`n" http://192.168.31.73/api/status
```

Linux / macOS:

```bash
curl -w "\nTIME: %{time_total}s\n" http://192.168.31.73/app.js -o /dev/null
curl -w "\nTIME: %{time_total}s\n" http://192.168.31.73/api/status
```

**Ожидание:** оба запроса отвечают быстро (BLE больше не делит радио с WiFi на главном узле).

---

## 0. I2C — делать первым

1. Перезагрузить главный ESP32, смотреть Serial
2. **Ожидание:** два скана — `I2C hypoxia` (22/23) и `I2C working` (18/19)
3. Отсутствующий адрес — проверить питание и подтяжки этой шины, не код

---

## 1. O₂ буфер гипоксии

1. **Ожидание:** плитка в группе «Буфер гипоксии»
2. Калибровка — [`firmwares/firmware_main_devkit/README.md`](firmwares/firmware_main_devkit/README.md)

---

## 1b. O₂ / давление рабочего контура

1. ADS1115 и DPS310 на шине 18/19 (те же адреса `0x48` / `0x76`)
2. **Ожидание:** плитки в группе «Рабочий контур»

---

## 2. Поток (SFM3300, рабочий контур, шина 18/19)

1. Плитка «Поток»: slm, фазы inhale/exhale
2. Продышать контур — знак потока меняется, VE растёт
3. «Сброс объёма» → `flow_reset` в логе

---

## 3. CO₂ (SCD41, рабочий контур, шина 18/19)

1. Первые ~10 с — «Прогрев»
2. В проветренном помещении 400–800 ppm
3. Выдох к датчику — рост на тысячи ppm

---

## 4. Давление (DPS310)

1. Гипоксия — плитка в «Буфер гипоксии»; рабочий — в «Рабочий контур»
2. Если `ok: false` — проверить SDO (0x76 vs 0x77) на этой шине

---

## 5. Wellue Ring O2 S

1. Кольцо в режиме **S8-AW** (не запись `T8520_xxxx`)
2. BLE-узел в зоне кольца; на main `sensors.bridge.ok`
3. **Ожидание:** в `/api/status` → `sensors.wellue.connected`, затем SpO₂/HR
4. В веб-UI — плитка Wellue; кнопка **Тренд** для графика

---

## 6. COOSPO H6M

1. Включить ремень, приложить датчик
2. **Ожидание:** `sensors.coospo.connected`, bpm, contact
3. R-R — отдельный канал тренда `rr`, если ремень отдаёт
4. **HRV:** на плитке ремня — **Замерить HRV** (кнопка активна при `connected`)
   - **30 сек** (отладка) / 2 / 3 / 5 мин → Старт → обратный отсчёт 5 с (ESP32 ещё не пишет)
   - затем прогресс, в конце Poincaré + SDNN / rMSSD / pNN50
   - На время замера UI не поллит `/api/status`
   - Отмена / ошибка / закрытие диалога сбрасывает сессию (`POST /api/hrv/stop`)
   - Краткий обрыв BLE до ~10 с не прерывает запись; дольше → «ремень отключён»
   - Повторный `POST /api/hrv/start` сбрасывает предыдущую сессию
   - `GET /api/hrv` в `done` содержит массив всех R-R, не 1 Гц тренд

---

## 7. WiFi STA

1. Кнопка **WiFi** → скан → SSID + пароль → сохранить
2. После reboot — доступ по IP в LAN (`wifi.sta.ip` в status)
3. AP остаётся для повторной настройки
4. Скан WiFi больше не должен ронять ремень (BLE на другой плате)

---

## 8. GPIO / клапан

1. GPIO Tester → pin 26 → ON / tap 3 с
2. **Ожидание:** реле срабатывает, в status `gpio` обновляется
3. Пины **16/17** в UI недоступны (UART)

---

## 9. Тренды

1. На плитке датчика — **Тренд**
2. Секция «Тренды»: графики 2/5/10/60/75 мин
3. **Сохранить** — PNG выбранного окна (в шапке файла указано окно; скрытые каналы на картинке нет)
4. Reboot обнуляет точки; маска каналов сохраняется в NVS

---

## 10. Действия и КП Бутейко

1. Между «Датчики» и «Тренды» — секция **Действия**: **Замерить HRV** и **Замер КП Бутейко**
2. КП открывает диалог; графики на главной странице продолжают писаться как обычно
3. В окне — таймер, SpO₂ / ЧСС, сетка **каждые 10 с** по вертикали от старта замера
4. **Завершить** — голубая линия конца КП на **обоих** графиках; при SpO₂ — ещё 45 с восстановления
5. **Сохранить PNG** в любой момент (сетка и маркер на картинке). **Закрыть** — прервать

---

## 10b. WiFi (STA)

1. Кнопка **WiFi** → **Скан** или ввод SSID вручную
2. **Ожидание:** список сетей за ~5–15 с; AP может кратко пропасть
3. При таймауте скана — ввести SSID вручную, сохранить
4. После сохранения — перезагрузка ESP; открыть UI по STA-IP или снова по AP `192.168.4.1`

---

## 11. Android (опционально)

1. `cd android && gradlew.bat assembleDebug`
2. **WiFi** → IP главного ESP32 (GATT на сплите нет)
3. GPIO/Servo, Wellue/COOSPO в карточке статуса
4. I2C-метрики — пока только в веб-UI
